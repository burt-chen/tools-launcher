# 個人工具啟動器(Launcher)設計文件

> 用一個小型 GUI launcher,讓內部同事/朋友從 GitHub Releases 下載各工具(exe),
> 一鍵安裝與啟動。類似迷你版的 Steam / Epic 啟動器。

---

## 1. 概念與情境

**問題**:你會做很多小工具(Excel 整理工具、檔名批次處理…),每個都是獨立的
PyInstaller exe。每次給人都要重發連結、解釋哪個是新版、舊版要刪掉。

**解法**:做一個「工具中心」啟動器:
- 你維護一份 `tools.json` 清單(放 GitHub Pages 或 Gist)
- 每個工具的 exe 用 GitHub Releases 發佈
- 使用者裝一次 Launcher,之後從清單裡挑工具下載、執行、更新

**目標使用者**:內部同事/朋友(信任群體,公開的 GitHub Releases 即可,不需登入)。

---

## 2. 系統架構

```
                       ┌──────────────────────────┐
                       │  GitHub                  │
                       │  ┌───────────────────┐   │
                       │  │ Pages / Gist      │   │
                       │  │ tools.json (清單) │   │
                       │  └─────────┬─────────┘   │
                       │            │             │
                       │  ┌─────────▼─────────┐   │
                       │  │ Releases:         │   │
                       │  │ excel-merger v1.2 │   │
                       │  │ filename-fixer v0.3│   │
                       │  │ ...               │   │
                       │  └───────────────────┘   │
                       └──────────────┬───────────┘
                                      │ HTTPS
                                      │
   ┌──────────────────────────────────▼──────────────────────────────────┐
   │  Launcher.exe (Tkinter, ~20 MB)                                     │
   │  ┌────────────────────────────────────────────────────────────────┐ │
   │  │ [刷新] [搜尋:______]                                            │ │
   │  │ ┌────────────────────────────────────────────────────────────┐ │ │
   │  │ │ ★ Excel 整理工具   v1.2.0  [已安裝 v1.1.0 → 更新可用] [啟動] │ │ │
   │  │ │   描述:篩選 + 彙整 xlsx/csv/json                          │ │ │
   │  │ │                                                            │ │ │
   │  │ │   檔名批次修改      v0.3.0  [未安裝]            [安裝]      │ │ │
   │  │ │   描述:正則替換、批次重新命名                              │ │ │
   │  │ │                                                            │ │ │
   │  │ │   ...                                                      │ │ │
   │  │ └────────────────────────────────────────────────────────────┘ │ │
   │  └────────────────────────────────────────────────────────────────┘ │
   └─────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ 安裝後寫入
                                      ▼
        %LOCALAPPDATA%\MyTools\
        ├─ installed.json
        ├─ excel-merger\
        │   └─ Excel整理工具.exe        ← 各工具自己解壓的本體
        └─ filename-fixer\
            └─ filename-fixer.exe
```

---

## 3. Catalog 格式(`tools.json`)

放在 GitHub Pages、Gist 或任何可公開讀取的靜態 URL。

```json
{
  "catalog_version": 1,
  "updated_at": "2026-05-14",
  "tools": [
    {
      "id": "excel-merger",
      "name": "Excel 整理工具",
      "description": "用一份 key 清單從母檔篩選彙整出新表,支援 xlsx/csv/json",
      "version": "1.2.0",
      "size_bytes": 18553807,
      "url": "https://github.com/USER/excel-merger/releases/download/v1.2.0/Excel整理工具.exe",
      "sha256": "abc123def456...",
      "category": "資料處理",
      "icon": "https://github.com/USER/excel-merger/raw/main/icon.png",
      "homepage": "https://github.com/USER/excel-merger",
      "changelog": "https://github.com/USER/excel-merger/releases/tag/v1.2.0"
    }
  ]
}
```

**必要欄位**:`id`(唯一識別)、`name`、`version`、`url`
**建議欄位**:`sha256`(驗證完整性)、`description`、`size_bytes`(顯示下載大小)
**選用欄位**:`icon`、`changelog`、`category`、`homepage`

---

## 4. 本地檔案結構

```
%LOCALAPPDATA%\MyTools\
├── installed.json     # 已安裝清單
└── tools\
    ├── excel-merger\
    │   ├── Excel整理工具.exe
    │   └── _meta.json   # {id, version, installed_at}
    └── filename-fixer\
        └── ...
```

`installed.json` 範例:
```json
{
  "excel-merger": {"version": "1.2.0", "path": "tools/excel-merger/Excel整理工具.exe", "installed_at": "2026-05-14T18:30:00"},
  "filename-fixer": {"version": "0.3.0", "path": "tools/filename-fixer/filename-fixer.exe", "installed_at": "2026-05-10T09:12:33"}
}
```

---

## 5. Launcher 核心功能

### MVP(必要功能)
- 啟動時 GET `tools.json`(快取一份在 `%LOCALAPPDATA%\MyTools\catalog_cache.json`,離線時用快取)
- 列出所有工具:名稱、版本、描述、狀態(未安裝 / 已安裝 / 有更新)
- 「安裝」:背景下載 exe → 驗 SHA256 → 解壓到 `tools/{id}/` → 寫入 `installed.json`
- 「啟動」:`subprocess.Popen([path])`
- 「移除」:刪除 `tools/{id}/`、從 `installed.json` 移除
- 下載進度條 + 取消

### 進階
- 自動檢查更新(背景每 6 小時)
- 搜尋 / 篩選分類
- 工具設定 / 版本回滾
- 多語系
- 群組(「我的最愛」)
- 自動更新 Launcher 本身

---

## 6. 開發步驟

### Phase 1:本地原型(1-2 天)
1. 用既有 Excel 工具的程式碼為基底,複製 `app/core.py`、`run.py`、`build.bat`
2. `app/main.py` 換成 launcher UI:
   - 上方:[刷新] [搜尋] [設定]
   - 中間:工具卡片清單(Listbox 或自訂 Frame)
   - 底部:狀態列 / 進度條 / 日誌(可選)
3. `app/catalog.py`:從 `tools.json` 載入清單(先用本地檔測試,之後換 URL)
4. `app/installer.py`:下載 + 驗 hash + 解壓 + 寫入 installed.json
5. `app/launcher_run.py`:`subprocess.Popen` 跑起工具

### Phase 2:接 GitHub(半天)
- 把 `tools.json` 放上 GitHub Pages 或 Gist
- 修改 catalog 載入用 `urllib.request` 抓 raw URL
- 加離線快取

### Phase 3:打磨(1-3 天)
- 進度條、錯誤處理
- 自動更新檢查
- Crash 不留垃圾(下載中斷時清掉殘檔)
- 圖示、品牌

---

## 7. 技術棧

| 元件 | 建議 |
|---|---|
| GUI | **Tkinter**(沿用 Excel 工具經驗,exe ~20 MB) |
| HTTP | `urllib.request`(stdlib,不用裝 requests) |
| Hash | `hashlib.sha256`(stdlib) |
| 並發 | `threading`(下載放背景,UI 主執行緒不卡) |
| 打包 | PyInstaller(沿用既有 build.bat) |
| Catalog 託管 | GitHub Pages(免費、HTTPS、CDN) |
| Releases 託管 | GitHub Releases(免費、各檔最大 2 GB) |

---

## 8. 工具發佈流程(你的 release 流程)

1. 在工具 repo 跑 `build.bat` 產 exe
2. 在工具 repo 開新 Release(tag `v1.2.0`),把 exe 拖上去當 asset
3. 算 SHA256:`certutil -hashfile dist\Excel整理工具.exe SHA256`
4. 編輯 catalog repo 的 `tools.json`,更新該工具的 `version` / `url` / `sha256`
5. push catalog repo → GitHub Pages 自動更新

可寫個小腳本自動化 step 3-5(讀 release URL → 算 hash → 更新 JSON → push)。

---

## 9. 注意事項

### Windows Defender 誤判
PyInstaller exe 沒簽名常被 Windows Defender 標記為「可能威脅」。對策:
- **免費**:第一次跑被擋時,使用者點「更多資訊 → 仍要執行」即可。可在 README 註明
- **要錢**:買 Code Signing Cert(~$200/年),或用免費的 [SignPath](https://signpath.io/) for open source
- 別用 UPX 壓縮,會大幅提高誤判率

### 自動更新 Launcher 本身
Windows 上正在執行的 exe 不能被覆蓋。常見解法:
- 下載新版到 `Launcher_new.exe` → 啟動一個小 batch script → batch 等舊版退出 → 改名覆蓋
- 或:每次啟動時檢查更新,提示使用者手動下載新版

### 工具完整性
- 一定要驗 SHA256(怕傳輸壞、怕被中間人偷換)
- 工具 exe 也建議數位簽章(若預算允許)

### 工具是否要互通資料
單檔工具最簡單,各管各的設定檔。若想統一(例如最近開的檔案、共用偏好):
- 工具寫到 `%APPDATA%\MyTools\{tool_id}\`
- Launcher 提供「全部備份」功能,把整個 `MyTools\` 打包

### 私有工具
若有公司內專屬工具不想公開:
- 用 private GitHub repo + GitHub Personal Access Token(需在 Launcher 內輸入儲存)
- 或:把 catalog 改成 Bearer Auth 的內部 URL(自建檔案伺服器)
- 不要把 token 寫死進 Launcher 程式碼

---

## 10. 最小可行原型骨架(Python 概念碼)

```python
# app/catalog.py
import json, urllib.request
from pathlib import Path

CATALOG_URL = "https://USER.github.io/my-tools/tools.json"
CACHE = Path.home() / "AppData/Local/MyTools/catalog_cache.json"

def fetch_catalog(force=False):
    try:
        with urllib.request.urlopen(CATALOG_URL, timeout=10) as r:
            data = json.loads(r.read())
        CACHE.parent.mkdir(parents=True, exist_ok=True)
        CACHE.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
        return data, "online"
    except Exception:
        if CACHE.exists():
            return json.loads(CACHE.read_text(encoding="utf-8")), "cached"
        raise

# app/installer.py
import hashlib, urllib.request, subprocess
from pathlib import Path

ROOT = Path.home() / "AppData/Local/MyTools/tools"
INSTALLED = ROOT.parent / "installed.json"

def install(tool, on_progress=None):
    dest = ROOT / tool["id"]
    dest.mkdir(parents=True, exist_ok=True)
    target = dest / Path(tool["url"]).name
    sha = hashlib.sha256()
    with urllib.request.urlopen(tool["url"]) as r, open(target, "wb") as f:
        total = tool.get("size_bytes") or 0
        downloaded = 0
        while chunk := r.read(64 * 1024):
            f.write(chunk)
            sha.update(chunk)
            downloaded += len(chunk)
            if on_progress and total:
                on_progress(downloaded, total)
    if "sha256" in tool and sha.hexdigest() != tool["sha256"]:
        target.unlink()
        raise ValueError("SHA256 mismatch")
    return target

def launch(installed_tool):
    subprocess.Popen([installed_tool["path"]])
```

從這些函式做起就好,UI 部分套到 Tkinter Listbox + Button 上即可。

---

## 11. 待決定的事

- [ ] Launcher 名稱(`MyTools.exe` / `工具中心.exe` / …)
- [ ] catalog 託管位置(GitHub Pages 子目錄 / 獨立 repo / Gist)
- [ ] 每個工具是否強制必要(`required: true`)
- [ ] 安裝資料夾位置:`%LOCALAPPDATA%`(無管理員)vs `%PROGRAMFILES%`(需管理員)
- [ ] 是否支援多版本並存
- [ ] Telemetry?(知道哪個工具最常用) — 涉及隱私要先問同事
